const form = document.getElementById('form')
const nome = document.getElementById('form')
const email = document.getElementById('form')
const password = document.getElementById('form')
const passwordtwo = document.getElementById('form')

form.addEventListener('submit', (e) => {
    e.preventDefault() //não permite que seja enviado o formulário preenchido
})


